# ==========================================================
# 🎯 Cursor 專案規範：Bootstrap 5 客製化開發（禁止亂改版）
# ==========================================================
# 📁 專案結構重點
#  - /scss/
#     ├─ _variables.scss        # 主題變數（淺色）
#     ├─ _variables-dark.scss   # 主題變數（深色）
#     ├─ _custom.scss           # 客製化元件樣式（依元件/區塊拆分）
#     └─ main.scss              # 入口檔案，僅用於 @import 與少量覆寫
#  - /css/main.css              # 編譯後輸出（禁止手動修改）
#  - /img/                      # 靜態資源
#  - package.json               # 腳本與套件（僅依規範修改）
# ==========================================================

meta:
  專案名稱: "Bootstrap5 客製化主題（防誤改版）"
  語言: "繁體中文"
  技術棧:
    - "Bootstrap 5"
    - "SCSS / Sass"
    - "Node.js（僅用於工具鏈）"
  開發目標:
    - "以 Bootstrap 變數覆寫與客製化 SCSS 為主"
    - "避免直接修改編譯輸出或第三方原始檔造成污染"
    - "維持結構清晰、命名一致、可維護"

# ----------------------------------------------------------
# 🧱 1. 保護與修改邊界
# ----------------------------------------------------------
guardrails:
  # ❌ 不可直接修改的檔案或資料夾
  唯讀:
    - "css/main.css"           # 編譯輸出檔
    - "node_modules/**"        # 第三方套件
    - ".git/**"
  
  # 🟡 僅允許在特定區塊編輯（使用旗標註記）
  局部可編輯:
    - 路徑: "index.html"
      可編輯區域:
        - "<!-- CURSOR:CONTENT_START -->"
        - "<!-- CURSOR:CONTENT_END -->"
        - "<!-- CURSOR:HEAD_EXTRA_START -->"
        - "<!-- CURSOR:HEAD_EXTRA_END -->"
    - 路徑: "scss/main.scss"
      可編輯區域:
        - "/* CURSOR:IMPORTS_START */"
        - "/* CURSOR:IMPORTS_END */"
        - "/* CURSOR:OVERRIDES_START */"
        - "/* CURSOR:OVERRIDES_END */"

  # ✅ 允許修改的檔案
  可寫入:
    - "scss/_variables.scss"
    - "scss/_variables-dark.scss"
    - "scss/_custom.scss"
    - "scss/main.scss"
    - "package.json"
    - ".gitignore"
    - "README.md"

# ----------------------------------------------------------
# ⚙️ 2. 編譯與指令規範
# ----------------------------------------------------------
commands:
  安裝: "npm i"
  開發模式: "npm run dev"
  打包模式: "npm run build"
  建議腳本模板:
    dev: "sass --watch scss/main.scss css/main.css --no-source-map"
    build: "sass scss/main.scss css/main.css --style=compressed --no-source-map"
    lint: "stylelint 'scss/**/*.scss' --fix || true"

# ----------------------------------------------------------
# 🎨 3. Bootstrap 客製化原則
# ----------------------------------------------------------
bootstrap_rules:
  變數覆寫流程:
    順序:
      - "在 scss/_variables.scss 覆寫 Bootstrap 變數（淺色主題）"
      - "在 scss/_variables-dark.scss 設定深色主題變數"
      - "在 main.scss 依序引入：變數檔 → Bootstrap → _custom.scss"
    禁止事項:
      - "不可直接修改 node_modules 內的 bootstrap 原始檔"
      - "不可在 _custom.scss 內大量定義變數（變數應回到 _variables*.scss）"
  檔案角色:
    "_variables.scss": "顏色、字型、間距、圓角、陰影、容器寬度等核心變數"
    "_variables-dark.scss": "深色主題變數，必要時使用 :root[data-theme='dark'] 包裝"
    "_custom.scss": "元件或區塊的自訂樣式（依區塊加註標題）"
    "main.scss": "入口檔案，僅負責 import 與少量全域覆寫"

# ----------------------------------------------------------
# 🧩 4. SCSS 寫作規範
# ----------------------------------------------------------
style_guide:
  命名規則:
    元件前綴: "c-"      # component
    工具前綴: "u-"      # utility
    版型前綴: "l-"      # layout
    範例:
      - ".c-card-pricing"
      - ".l-header"
      - ".u-text-muted"
  結構規範:
    - "每個區塊前加上 === Section: 名稱 === 作為註解標題"
    - "巢狀深度不超過三層"
    - "避免使用 !important（必要時註明原因）"
  響應式規範:
    - "統一使用 Bootstrap mixin，例如：@include media-breakpoint-up(md) { ... }"
  顏色變數:
    - "統一使用 $primary/$secondary/$success/$warning/$danger 等"
    - "新增品牌色時使用 $brand-* 命名並於 _variables.scss 定義"

# ----------------------------------------------------------
# 🤖 5. 允許 Cursor 執行的動作
# ----------------------------------------------------------
cursor_can:
  - "在允許區塊新增元件範例（卡片、按鈕群組、Navbar、表單等）"
  - "於 _variables*.scss 調整顏色、圓角、字級、陰影"
  - "於 _custom.scss 新增或擴充元件樣式，遵守命名規範"
  - "補齊 README.md 的使用說明與維護步驟"
  - "若缺少 npm 腳本，可依建議模板新增"

# ----------------------------------------------------------
# ⛔️ 6. 禁止 Cursor 執行的動作
# ----------------------------------------------------------
cursor_must_not:
  - "修改 css/main.css（任何情況皆禁止）"
  - "變更或刪除 node_modules 內檔案"
  - "刪除或改名 scss 既有檔案"
  - "修改 index.html 未標示的區域"
  - "引入與 Bootstrap 衝突的 Reset 或大型 UI 套件"
  - "使用非專案規範的 class 前綴"
  - "新增顏色但未在 _variables.scss 建立對應 token"

# ----------------------------------------------------------
# 📦 7. 產出格式與操作規範
# ----------------------------------------------------------
output_format:
  程式區塊格式化: true
  說明前置: false
  小型修改時:
    - "先列出『修改項目清單』"
    - "僅輸出必要片段（不要貼整檔）"
  主題修改時:
    - "只允許調整 _variables.scss / _variables-dark.scss"
    - "附上修改的變數清單與影響範圍"

# ----------------------------------------------------------
# 📝 8. Git 提交訊息建議
# ----------------------------------------------------------
commit_messages:
  模板:
    - "style(vars): 調整主題色與圓角"
    - "feat(ui): 新增 .c-card-pricing 元件樣式"
    - "fix(rwd): 修正 md 斷點下 Navbar 對齊"
    - "chore(build): 補上 sass 腳本與樣式檢查"

# ----------------------------------------------------------
# 💬 9. 問答提示（Cursor 幫助指引）
# ----------------------------------------------------------
qa_hints:
  - "若要更新色系，僅修改 _variables.scss 或 _variables-dark.scss 並列出變數清單"
  - "若要新增卡片元件，請於 _custom.scss 以『=== Section: 名稱 ===』開頭"
  - "若要調整 RWD，請使用 Bootstrap 的 mixin，不要直接寫 px 斷點"
